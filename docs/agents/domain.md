# Domain documentation

Use a single root `CONTEXT.md` and `docs/adr/` for domain terminology and architectural decisions. Read existing context and relevant ADRs before exploring an area. If they do not exist, continue without creating placeholders.

Use the glossary's terminology in tests, issues, and proposals. Call out conflicts with existing ADRs before changing their decisions. Domain modeling creates these documents when terminology or decisions need recording.
